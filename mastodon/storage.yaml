apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: mastodon
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti

