apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: mastodon
spec:
  storageClassName: longhorn-xfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Gi
