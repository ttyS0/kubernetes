apiVersion: kubedb.com/v1alpha1
kind: Snapshot
metadata:
  name: es-snapshot
  namespace: elk
  labels:
    kubedb.com/kind: Elasticsearch
spec:
  databaseName: es
  local:
    mountPath: /repo
    persistentVolumeClaim:
      claimName: es-bkp

