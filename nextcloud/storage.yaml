apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: base
  namespace: nextcloud
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Ti
