apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: nextcloud
spec:
  storageClassName: pool
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
