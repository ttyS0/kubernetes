apiVersion: v1
kind: Service
metadata:
  name: wikijs
  namespace: wikijs
  labels:
    instance: wikijs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    instance: wikijs
