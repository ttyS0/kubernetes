apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: docker-tls
  namespace: docker
spec:
  secretName: docker-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: docker.ttys0.net
  dnsNames:
    - docker.ttys0.net
