apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud
  namespace: media
spec:
  storageClassName: pool
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Ti
