apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: skj-tls
  namespace: bookstack
spec:
  secretName: skj-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: skj.wiki
  dnsNames:
    - skj.wiki
