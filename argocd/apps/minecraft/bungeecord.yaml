apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bungeecord
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://git.skj.dev/sean/kubernetes.git'
    path: minecraft/bungeecord
    targetRevision: HEAD
  destination:
    namespace: minecraft
    server: https://kubernetes.default.svc
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
