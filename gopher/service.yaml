apiVersion: v1
kind: Service
metadata:
  name: gopher
  namespace: gopher
  labels:
    app: gopher
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
    - port: 70
      name: gopher
      targetPort: 70
  selector:
    app: gopher
