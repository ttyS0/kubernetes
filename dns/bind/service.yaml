apiVersion: v1
kind: Service
metadata:
  name: bind
  namespace: dns
  labels:
    app: bind
spec:
  type: LoadBalancer
  ports:
    - name: bind
      port: 53
      targetPort: 53
      protocol: UDP
  selector:
    app: bind
