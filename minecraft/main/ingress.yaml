apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: map-main
  namespace: minecraft
  annotations:
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: "nginx-internal"
  rules:
    - host: mc-main.skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: main
                port:
                  number: 8100
  tls:
    - hosts:
        - mc-main.skj.dev
      secretName: mc-main-skj-dev-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: map-main-ext
  namespace: minecraft
spec:
  ingressClassName: "nginx-external"
  rules:
    - host: mc-main.skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: main
                port:
                  number: 8100
  tls:
    - hosts:
        - mc-main.skj.dev
      secretName: mc-main-skj-dev-cert
