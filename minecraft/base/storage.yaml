apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worlds
  namespace: minecraft
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Ti
