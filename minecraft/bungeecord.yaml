apiVersion: apps/v1
kind: Deployment
metadata:
  name: bungeecord
  namespace: minecraft
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bungeecord
  template:
    metadata:
      labels:
        app: bungeecord
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - imagePullPolicy: IfNotPresent
          name: bungeecord
          image: ttys0/bungeecord:v1513
          workingDir: "/minecraft/worlds/bungeecord"
          ports:
            - containerPort: 25577
          volumeMounts:
            - name: worlds
              mountPath: "/minecraft/worlds"
          env:
            - name: JVM_OPTS
              value: "-Xms2G -Xmx2G"
            - name: BUNGEE_HOME
              value: "/minecraft/worlds/bungeecord"
      volumes:
        - name: worlds
          persistentVolumeClaim:
            claimName: minecraft-worlds
---
apiVersion: v1
kind: Service
metadata:
  name: bungeecord
  namespace: minecraft
  labels:
    app: bungeecord
spec:
  type: LoadBalancer
  ports:
    - name: server
      port: 25565
      targetPort: 25577
  selector:
    app: bungeecord
