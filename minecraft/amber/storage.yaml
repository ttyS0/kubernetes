apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amber
  namespace: minecraft
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
