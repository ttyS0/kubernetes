namespace: minecraft

resources:
  - ../base/world
  - ingress.yaml

patchesJson6902:
  - target:
      kind: Deployment
      name: world
      version: v1
    path: deploymentPatch.yaml
  - target:
      kind: Service
      name: world
      version: v1
    path: servicePatch.yaml

images:
  - name: ghcr.io/ttys0/spigot
    newTag: 1.18.2

