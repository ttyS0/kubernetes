apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minecraft
  namespace: minecraft
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  rules:
    - host: minecraft.ttys0.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: amber
                port:
                  number: 8123
  tls:
    - hosts:
        - minecraft.ttys0.net
      secretName: minecraft-ttys0-net-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minecraft-ext
  namespace: minecraft
  annotations:
    kubernetes.io/ingress.class: nginx-external
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: minecraft.ttys0.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: amber
                port:
                  number: 8123
  tls:
    - hosts:
        - minecraft.ttys0.net
      secretName: minecraft-ttys0-net-cert
