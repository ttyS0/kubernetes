apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: map-rebma
  namespace: minecraft
  annotations:
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: "nginx-internal"
  rules:
    - host: mcrebma.skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rebma
                port:
                  number: 8123
  tls:
    - hosts:
        - mcrebma.skj.dev
      secretName: minecraft-rebma-skj-dev-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: map-rebma-ext
  namespace: minecraft
spec:
  ingressClassName: "nginx-external"
  rules:
    - host: mcrebma.skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rebma
                port:
                  number: 8123
  tls:
    - hosts:
        - mcrebma.skj.dev
      secretName: minecraft-rebma-skj-dev-cert
