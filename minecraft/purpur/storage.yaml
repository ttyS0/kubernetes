apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: world
  namespace: minecraft
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
