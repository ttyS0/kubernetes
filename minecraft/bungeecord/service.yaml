apiVersion: v1
kind: Service
metadata:
  name: bungeecord
  namespace: minecraft
  labels:
    app: bungeecord
spec:
  type: LoadBalancer
  ports:
    - name: server
      port: 25565
      targetPort: 25577
  selector:
    app: bungeecord
