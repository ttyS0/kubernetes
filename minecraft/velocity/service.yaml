apiVersion: v1
kind: Service
metadata:
  name: velocity
  namespace: minecraft
  labels:
    app: velocity
spec:
  type: LoadBalancer
  ports:
    - name: server
      port: 25565
      targetPort: 25577
  selector:
    app: velocity
