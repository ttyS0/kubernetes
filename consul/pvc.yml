kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: data-consul-0
  namespace: consul
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: data-consul-1
  namespace: consul
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
 name: data-consul-2
 namespace: consul
spec:
 storageClassName: rbd
 accessModes:
   - ReadWriteOnce
 resources:
   requests:
     storage: 10Gi
