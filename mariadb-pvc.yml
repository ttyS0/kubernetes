kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb-storage
  namespace: mariadb
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi

