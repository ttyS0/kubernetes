kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dynamodb-data
  namespace: dynamodb
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
