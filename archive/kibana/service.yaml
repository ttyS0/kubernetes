apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: ingress-nginx
  labels:
    name: kibana
spec:
  ports:
    - name: server
      port: 5601
  selector:
    app: kibana
  type: NodePort