kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: webdav-data
  namespace: ingress-nginx
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
