apiVersion: v1
kind: Service
metadata:
  name: webdav
  namespace: ingress-nginx
  labels:
    name: webdav
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: webdav
