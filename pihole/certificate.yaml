apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: pihole-tls
  namespace: pihole
spec:
  secretName: pihole-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: pihole.ttys0.net
  dnsNames:
    - pihole.ttys0.net
