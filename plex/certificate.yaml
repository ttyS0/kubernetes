apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: plex-tls
  namespace: plex
spec:
  secretName: plex-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: plex.ttys0.net
  dnsNames:
    - plex.ttys0.net
