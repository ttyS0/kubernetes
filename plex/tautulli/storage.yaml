apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-data
  namespace: plex
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
