apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: vault-ttys0-tls
  namespace: vault
spec:
  secretName: vault-ttys0-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: vault.ttys0.net
  dnsNames:
    - vault.ttys0.net




