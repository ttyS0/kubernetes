apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: vault-tls
  namespace: vault
spec:
  secretName: vault-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: vault.ttys0.net
  dnsNames:
    - vault.ttys0.net




