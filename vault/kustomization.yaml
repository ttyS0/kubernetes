namespace: vault

resources:
  - vault.yaml
  - vault-unseal.yaml

patchesStrategicMerge:
  - vault-server-test.patch.yaml

images:
  - name: hashicorp/vault
    newTag: 1.11.2
  - name: hashicorp/vault-k8s
    newTag: 0.17.0
