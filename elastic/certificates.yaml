apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: kibana-tls
  namespace: elastic
spec:
  secretName: kibana-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: 'kibana.ttys0.net'
  dnsNames:
    - kibana.ttys0.net
