apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: elasticsearch
  namespace: elastic
spec:
  secretName: elasticsearch-tls
  duration: 2160h
  renewBefore: 1920h
  commonName: instance
  issuerRef:
    name: vault
    kind: Issuer
---
