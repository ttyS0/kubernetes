kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb-data
  namespace: db
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
