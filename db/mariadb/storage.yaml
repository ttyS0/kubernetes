kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb
  namespace: db
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
