kind: Deployment
apiVersion: apps/v1
metadata:
  name: postgres
  namespace: db
  labels:
    app: postgres
spec:
  selector:
    matchLabels:
      app: postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgres
    spec:
      securityContext:
        runAsUser: 999
      containers:
        - image: postgres:latest
          name: postgres
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pgdata
                  key: pgroot
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: PGSSLMODE
              value: "prefer"
            - name: PGSSLKEY
              valueFrom:
                secretKeyRef:
                  key: tls.key
                  name: pg-skj-dev-cert
            - name: PGSSLCERT
              valueFrom:
                secretKeyRef:
                  key: tls.crt
                  name: pg-skj-dev-cert
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pg
