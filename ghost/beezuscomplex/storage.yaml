kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ghost
  namespace: beezuscomplex-data
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
