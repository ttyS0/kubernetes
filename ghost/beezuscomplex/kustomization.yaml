namespace: ghost

resources:
  - ../base
  - ingress.yaml
  - configmap.yaml
  - secret.yaml

patchesJson6902:
  - target:
      kind: Deployment
      name: ghost
      version: v1
    path: deployPatch.yaml
  - target:
      kind: Service
      name: ghost
      version: v1
    path: namePatch.yaml

images:
  - name: library/ghost
    #newName: git.skj.dev/sean/ghost
    newTag: 5.45.1

commonLabels:
  app: beezuscomplex
