namespace: ghost

resources:
  - ../base
  - ingress.yaml
  - configmap.yaml
  - secret.yaml

patchesJson6902:
  - target:
      kind: PersistentVolumeClaim
      name: ghost
      version: v1
    path: namePatch.yaml
  - target:
      kind: Deployment
      name: ghost
      version: v1
    path: deployPatch.yaml
  - target:
      kind: Service
      name: ghost
      version: v1
    path: namePatch.yaml

images:
  - name: library/ghost
    newName: hub.skj.dev/img/ghost
    newTag: 5.2.1

commonLabels:
  app: splashofink
