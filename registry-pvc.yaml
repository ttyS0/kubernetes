kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: docker-registry-data
  namespace: ingress-nginx
spec:
  storageClassName: rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
