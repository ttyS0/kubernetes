apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: skj-dev
  namespace: web
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  rules:
    - host: skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hugo
                port:
                  number: 80
  tls:
    - hosts:
        - skj.dev
        - www.skj.dev
      secretName: skj-dev-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: skj-dev-external
  namespace: web
  annotations:
    kubernetes.io/ingress.class: nginx-external
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  rules:
    - host: skj.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hugo
                port:
                  number: 80
  tls:
    - hosts:
        - skj.dev
        - www.skj.dev
      secretName: skj-dev-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 108minutes-net
  namespace: web
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  rules:
    - host: 108minutes.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hugo
                port:
                  number: 80
  tls:
    - hosts:
        - 108minutes.net
        - www.108minutes.net
      secretName: 108minutes-net-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 108minutes-net-external
  namespace: web
  annotations:
    kubernetes.io/ingress.class: nginx-external
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  rules:
    - host: 108minutes.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hugo
                port:
                  number: 80
  tls:
    - hosts:
        - 108minutes.net
        - www.108minutes.net
      secretName: 108minute-net-cert
