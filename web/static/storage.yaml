apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static
  namespace: web
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
