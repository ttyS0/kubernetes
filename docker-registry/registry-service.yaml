apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: ingress-nginx
  labels:
    app: docker-registry
spec:
  ports:
  - name: http
    port: 5000
    targetPort: 5000
  selector:
    app: docker-registry
