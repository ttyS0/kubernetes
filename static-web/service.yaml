apiVersion: v1
kind: Service
metadata:
  name: static-web
  namespace: ingress-nginx
  labels:
    app: static-web
spec:
  ports:
  - name: server
    port: 80
    targetPort: 80
  selector:
    app: static-web
