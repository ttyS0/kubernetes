---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql-storage
  namespace: postgresql
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: postgresql
  namespace: postgresql
  labels:
    app: postgresql
spec:
  selector:
    matchLabels:
      app: postgresql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
      - image: postgres:10.5
        name: postgresql
        env:
        # kubectl create secret generic postgres-passwd --from-literal=passwd=ROOT_PASSWORD
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-passwd
              key: passwd
        ports:
        - containerPort: 5432
          name: postgresql
        volumeMounts:
        - name: postgresql-storage
          mountPath: /var/lib/postgresql
      volumes:
      - name: postgresql-storage
        persistentVolumeClaim:
          claimName: postgresql-storage

---
kind: Service
apiVersion: v1
metadata:
  name: postgresql
  namespace: postgresql
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.1.203
  selector:
    app: postgresql
  ports:
  - name: postgresql
    protocol: TCP
    port: 5432
    targetPort: 5432
